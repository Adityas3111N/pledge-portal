---
description: Frontend implementation standards
globs: ["src/**/*.{ts,tsx}"]
alwaysApply: true
---

# Components

Prefer:

- Server Components

Use Client Components only when necessary.

# Forms

Always use:

- React Hook Form
- Zod validation

Never use useState for large forms.

# Tables

- Use reusable table components
- Support loading states
- Support empty states
- Support error states

# Accessibility

Always:

- Label inputs
- Use semantic HTML
- Support keyboard navigation
- Provide aria attributes when needed

# Styling & Responsiveness

Use:

- Tailwind utilities
- Design system tokens
- Fluid responsive layouts (`max-w-[1280px]` container grids) to ensure seamless rendering across:
  - Mobile screens (320px+)
  - Standard/older government office monitors (1024x768, 1280x1024 resolutions)
  - Desktop widescreen monitors and Macs (1440x900, 1920x1080, and high-res Retina displays)

Avoid:

- Inline styles
- Magic numbers
- Hardcoded colors
- Fixed element widths that cause horizontal scrolling on smaller screens or visual breaking on low-res screens

# Testing

For business logic:

- Write unit tests

For API integrations:

- Write integration tests

For critical user journeys:

- Write E2E tests

Always test:

- Validation
- Permissions
- Data transformations
- Complex calculations

Do not write tests for:
- Simple presentational components
- Third-party libraries
- Framework behavior